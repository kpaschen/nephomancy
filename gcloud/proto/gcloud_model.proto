// Gcloud-specific extensions for the asset model

syntax = "proto3";

package model;
option go_package = ".;assets";

message GCloudVM {
  string machine_type = 1;
  string region = 2;
  string zone = 3;
  string scheduling = 4;
  string network_tier = 5;
}

message GCloudDisk {
  string disk_type = 1;
  bool is_regional = 2;
  string region = 3;
  string zone = 4;  // only for zonal disks
}

message GCloudSubnetwork {
  string tier = 1;
  string region = 2;
}

message GCloudImage {
  string region = 1;
}

